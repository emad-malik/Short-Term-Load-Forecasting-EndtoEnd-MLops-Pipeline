# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
*.egg-info/
dist/
build/

# Jupyter Notebook
.ipynb_checkpoints

# Data files
data/raw/*.zip
data/raw/*.csv
data/processed/*.csv

# Models
# NOTE: xgboost_demand_model.pkl, feature_names.pkl, label_encoders.pkl, and
# model_metrics.pkl are intentionally tracked (not ignored) so Render's build
# has a trained model to load — see DEPLOYMENT_RENDER.md section 3, Option A.
# Re-add exclusions below only if you switch to Option B/C instead.
# models/*.pkl
# models/*.joblib

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Static files
app/static/*.png
app/static/*.jpg
app/static/*.jpeg
